spring:
  cloud:
    config:
      name: microservicecloud-config-dept-client #需要从github上读取的资源名称
      profile: dev
      label: master
      uri: http://config-3344.com:3344 #本服务启动后先去找3344号服务，通过SpringCloudConfig获取github的服务地址
